<%= form_with(model: citum, local: true) do |form| %>
  <% if citum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(citum.errors.count, "error") %> prohibited this citum from being saved:</h2>

      <ul>
      <% citum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.datetime_select :fecha, id: :citum_fecha %>
  </div>

  <div class="field">
    <%= form.label :doctor_id %>
    <%= form.select :doctor_id, Doctor.all.collect {|ps| [ps.name, ps.id]} %>
  </div>

  <div class="field">
    <%= form.label :paciente_id %>
    <%= form.select :paciente_id, Paciente.all.collect {|ps| [ps.name, ps.id]} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
